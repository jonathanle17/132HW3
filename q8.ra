\rename_{borrowedAmount} (
  \project_{total} (
    \rename_{name, total} (
      \aggr_{name : sum(amount)} (
        (\select_{name = 'John Smith'} customer)
        \lojoin_{name = cname}
        (borrower \join_{lno = no} loan)
      )
    )
  )
);