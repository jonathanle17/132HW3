(
  \rename_{name} (
    \project_{cname} (
      borrower \join_{lno = no} (
        \select_{type = 'jumbo mortgage'} loan
        )
    )
  )
)
\intersection
(
  \project_{name} (
    borrower \join_{lno = no} (
        \select_{type = 'student'} loan
        )
    )
);