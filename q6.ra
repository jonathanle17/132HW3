\rename_{name} (
  \project_{cname} (
    \select_{total >= 13000} (
        \rename_{cname, total} (
            \aggr_{cname : sum(amount)} (
                borrower \join_{lno = no} loan
            )
        )
    )
  )
);